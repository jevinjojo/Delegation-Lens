# API
API_BIND_ADDRESS=127.0.0.1:8080
DASHBOARD_ORIGIN=http://127.0.0.1:5173

# Database
DATABASE_URL=sqlite://delegation_lens.db

# Chain / RPC — needed from Phase 7 onward; leave blank for fixture-only work
CHAIN_ID=11155111
RPC_HTTP_URL=
RPC_WS_URL=
CONFIRMATION_DEPTH=12

# Logging
RUST_LOG=info,delegation_lens=debug

# Block to begin backfill from when the DB is empty (set to a recent Sepolia block!)
START_BLOCK=0